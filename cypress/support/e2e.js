// Loaded automatically before every spec. Registers the Mochawesome
// reporter's screenshot/log hooks; no custom Cypress commands are
// currently needed by this suite.
import 'cypress-mochawesome-reporter/register';
